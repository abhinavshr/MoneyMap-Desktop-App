<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:MoneyMap"
       x:Class="MoneyMap.AppShell">

    <!-- Dashboard page -->
    <ShellContent Title="Dashboard" Route="Dashboard" ContentTemplate="{DataTemplate local:DashboardPage}" />

    <!-- Cash Inflows page -->
    <!-- <ShellContent Title="Cash Inflows" Route="CashInflows" ContentTemplate="{DataTemplate local:CashInflowsPage}" /> -->

    <!-- Cash Outflows page -->
    <!-- <ShellContent Title="Cash Outflows" Route="CashOutflows" ContentTemplate="{DataTemplate local:CashOutflowsPage}" /> -->

    <!-- Debt Tracking page -->
    <!-- <ShellContent Title="Debt Tracking" Route="DebtTracking" ContentTemplate="{DataTemplate local:DebtTrackingPage}" /> -->
</Shell>
